# Vercel KV Configuration
# Copy this file to .env.local and fill in your actual values

# KV Database URL (Redis connection string)
KV_URL=redis://username:password@host:port

# KV REST API URL
KV_REST_API_URL=https://your-project.vercel-storage.com

# KV REST API Token (for read/write operations)
KV_REST_API_TOKEN=your_vercel_kv_rest_api_token_here

# KV REST API Read-Only Token (for read-only operations)
KV_REST_API_READ_ONLY_TOKEN=your_vercel_kv_read_only_token_here

# Note: These values will be automatically populated when you run:
# vercel env pull .env.local
